function obtenerLargoDelArray(array) {
  // Retornar la longitud del arreglo recibido.
  // Tu código:
 return array.length;
}

module.exports = obtenerLargoDelArray;

/*
1El método push() añade uno o más elementos al final de un arreglo, y devuelve la nueva longitud del array.

2
El método pop() elimina y devuelve el último elemento de un arreglo.

3
El método unshift() agrega uno o más elementos al inicio de un arreglo, y devuelve la nueva longitud del array.

4
El método shift() elimina y devuelve el primer elemento

*/